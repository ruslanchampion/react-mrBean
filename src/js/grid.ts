class grid extends Array {
  constructor(arr: Array<number>){
    super(...arr);
  }
  getNeighbors(coord: {r: number, c: number}) {

  }
  getDistanceTable(start: {r: number, c: number}) {
    
  }
}